---
// src/components/Footer.astro
const { setmoreUrl, instagramHandle } = Astro.props;
---

<footer class="bg-zinc-950 border-t border-white/5 pt-20 pb-24 md:pb-10">
  <div class="container-custom">
    <div
      class="flex flex-col md:flex-row justify-between items-start gap-12 mb-16"
    >
      <!-- Brand Column -->
      <div class="space-y-6 max-w-sm">
        <h2 class="text-xl font-serif text-white uppercase tracking-widest">
          Barbería <span class="text-gold">Demo</span>
        </h2>
        <p class="text-zinc-500 text-sm leading-relaxed">
          Respetando la tradición de la barbería clásica. <br />
          Tu espacio de confianza para el cuidado personal.
        </p>
      </div>

      <!-- Links Column -->
      <div class="flex gap-16">
        <div class="space-y-4">
          <h3 class="text-xs font-bold uppercase tracking-widest text-gold">
            Contacto
          </h3>
          <nav class="flex flex-col gap-3">
            <a
              href={setmoreUrl}
              target="_blank"
              class="text-sm text-zinc-400 hover:text-white transition-colors"
            >
              Agendar Cita
            </a>
          </nav>
        </div>

        <div class="space-y-4">
          <h3 class="text-xs font-bold uppercase tracking-widest text-gold">
            Social
          </h3>
          <nav class="flex flex-col gap-3">
            <a
              href="#"
              target="_blank"
              class="text-sm text-zinc-400 hover:text-white transition-colors"
            >
              Instagram
            </a>
            <a
              href="#"
              target="_blank"
              class="text-sm text-zinc-400 hover:text-white transition-colors"
            >
              Facebook
            </a>
          </nav>
        </div>
      </div>
    </div>

    <!-- Bottom Bar -->
    <div
      class="border-t border-white/5 pt-8 flex flex-col md:flex-row justify-between items-center gap-4"
    >
      <p class="text-xs text-zinc-600 uppercase tracking-wider">
        &copy; {new Date().getFullYear()} Barbería Demo. Todos los derechos reservados.
      </p>
      <p class="text-xs text-zinc-700">
        Diseño Demo por <span class="text-zinc-500"
          >JAVER Website Managing Solutions</span
        >
      </p>
    </div>
  </div>
</footer>
