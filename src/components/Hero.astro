---
// src/components/Hero.astro
const { setmoreUrl } = Astro.props;
---

<section
  class="relative flex min-h-[85vh] w-full items-center justify-center overflow-hidden bg-zinc-900 pt-16"
>
  <!-- Background Image with Overlay -->
  <div class="absolute inset-0 z-0">
    <div
      class="absolute inset-0 z-10 bg-[radial-gradient(circle_at_center,_var(--tw-gradient-stops))] from-zinc-800/50 via-zinc-900 to-zinc-950"
    >
    </div>
    <!-- Dark overlay -->
    <!-- Assuming hero.jpg or similar exists, otherwise fallback to bg color -->

    <!-- If no image is strictly defined in prompt, rely on color or generic placeholder if provided. 
          Prompt said "Background image (darkened via CSS overlay)". 
          I will try to use a standard placeholder path if no specific file was mentioned,
          or rely on the solid dark vibe if image is missing. 
          Let's assume a generic class or inline style that can load an image if present. -->
  </div>

  <div
    class="container-custom relative z-20 flex flex-col items-center text-center"
  >
    <!-- Hero Logo -->
    <div
      class="mb-6 opacity-0 animate-fade-in-up md:max-w-md transition-all duration-1000"
    >
      <h2
        class="text-4xl md:text-6xl font-serif text-white uppercase tracking-[0.3em]"
      >
        Barbería <span class="text-gold">Demo</span>
      </h2>
    </div>

    <!-- Value Prop -->
    <h1
      class="font-serif text-2xl font-light leading-tight text-zinc-100 md:text-4xl lg:text-5xl max-w-2xl mx-auto opacity-0 animate-fade-in-up stagger-1"
      style="animation-fill-mode: forwards;"
    >
      <span
        class="block text-gold mb-2 text-lg md:text-xl uppercase tracking-[0.2em] font-sans font-medium"
        >Cuidado & Tradición</span
      >
      Cortes impecables, afeitado clásico y la mejor experiencia para el caballero.
    </h1>

    <!-- CTA -->
    <div
      class="mt-8 md:mt-10 flex flex-col gap-4 sm:flex-row opacity-0 animate-fade-in-up stagger-2"
      style="animation-fill-mode: forwards;"
    >
      <a
        href={setmoreUrl}
        target="_blank"
        rel="noreferrer"
        class="min-w-[180px] bg-gold py-3.5 px-8 text-center text-sm font-bold uppercase tracking-widest text-zinc-900 transition-all duration-300 hover:bg-white hover:text-zinc-900 shadow-xl hover:-translate-y-0.5"
      >
        Agendar Cita
      </a>
    </div>
  </div>

  <style>
    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .animate-fade-in-up {
      animation: fadeInUp 0.8s ease-out forwards;
    }
  </style>
</section>
